Changes:
- Removed the hook that removed finalizers from the VolumeSnapshot, VolumeSnapshotContent
  and VolumeSnapshotClass resources when the module was turned off. Turning off the
  module no longer affects these resources, making it safe to transfer snapshot support
  to the storage-foundation module.
- Removed the unused snapshot-validation-webhook certificate hook and its values ​​scheme.
